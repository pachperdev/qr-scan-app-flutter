import 'package:flutter/material.dart';

class DirectionHistory extends StatelessWidget {
  const DirectionHistory({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('DirectionHistory'));
  }
}
